apiVersion: v1
kind: Service
metadata:
  name: nginx-backend
spec:
  type: ClusterIP
  ports:
      # mandatopry field
    - targetPort: 80
      # If you don't provide a target port, it will use the same as port.
      port: 80
  selector:
    app: nginx
    type: front-end
